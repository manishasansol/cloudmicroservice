spring:
  application:
    name: licensingservice
  profiles:
    active:
      default
  cloud:
    config:
      enabled: false
#      discovery:
#        enabled: true
#        serviceId: configserver
